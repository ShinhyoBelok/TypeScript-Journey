const superHero = [] //make it never type so you can't push data to array 

const arrayElements: string[] = [];

superHero.push("");
arrayElements.push("");

let arArElement: string[][] = [
  ["klk", "dsfa", "asdfg"]
]